function largestOfFour(arr) {
  // You can do this!
  return arr;
}

largestOfFour([[4, 5, 1, 3], [13, 27, 18, 26], [32, 35, 37, 39], [1000, 1001, 857, 1]]);

/*
create largestNumbersArray to []
define compareNum function with two parameters, biggestNum and currentNum.
return biggest number

outer for loop that iterates thru each element in 
largestOfFour

inner loop that iterates thru each element of elements
create biggestNum variable to 0;
create currentNum variable to element[i]
pass biggestNum and currentNum to compareNum function and push returned value to largestNumberArray.

*/